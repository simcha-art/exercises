<div dir="rtl">

# Import / Export – CommonJS & ES6 Modules – תרגילים

---

## חלק 1 – CommonJS (require / module.exports)

---

### רמה קלה

---

### תרגיל 1

צור קובץ `math.js` שמייצא פונקציה שמחברת שני מספרים.

ייבא אותה לקובץ אחר והדפס תוצאה.

---

### תרגיל 2

צור קובץ `greet.js` שמייצא פונקציה שמקבלת שם ומחזירה:

</div>

```
Hello Moshe
```

<div dir="rtl">

ייבא והשתמש בה.

---

### תרגיל 3

צור קובץ שמייצא אובייקט עם:

- `name`
- `age`

ייבא והדפס את הערכים.

---

### תרגיל 4

צור קובץ עם 2 פונקציות — חיבור וכפל.

ייצא את שתיהן והשתמש בהן בקובץ אחר.

---

### רמה בינונית

---

### תרגיל 5

צור קובץ `utils.js` עם 3 פונקציות:

- חיבור
- חיסור
- כפל

ייבא את כולן והשתמש רק באחת.

---

### תרגיל 6

צור קובץ שמייצא פונקציה שבודקת אם מספר זוגי.

ייבא אותה והרץ על כמה מספרים.

---

### תרגיל 7

צור קובץ שמייצא מחלקה `User` עם `name`.

ייבא וצור אובייקט חדש.

---

### תרגיל 8

צור קובץ שמייצא מערך של מספרים.

ייבא וסכם את כל המספרים.

---

### רמה קשה

---

### תרגיל 9

צור 2 קבצים: `math.js` ו-`advancedMath.js`.

`advancedMath` משתמש ב-`math` ומוסיף פונקציה חדשה.

ייבא והשתמש בקובץ שלישי.

---

### תרגיל 10

צור מערכת קטנה:

- `logger.js` — מדפיס הודעות
- `app.js` — משתמש ב-`logger`

הוסף סוגי הודעות: `info` / `error`

---

## חלק 2 – ES6 Modules (import / export)

---

### רמה קלה

---

### תרגיל 1

צור קובץ `math.js` עם:

</div>

```javascript
export const sum = (a, b) => a + b;
```

<div dir="rtl">

ייבא והשתמש.

---

### תרגיל 2

צור פונקציה `greet` עם `export` רגיל (named).

ייבא אותה והדפס.

---

### תרגיל 3

צור `export` של משתנה `PI`.

ייבא והשתמש.

---

### תרגיל 4

צור `default export` של פונקציה שמכפילה מספר.

ייבא והשתמש.

---

### רמה בינונית

---

### תרגיל 5

צור קובץ עם 3 פונקציות (named export).

ייבא רק אחת מהן.

---

### תרגיל 6

צור `default export` של אובייקט `user`.

ייבא והדפס `name`.

---

### תרגיל 7

צור מחלקה `Car` עם `export`.

ייבא וצור instance.

---

### תרגיל 8

צור קובץ עם `default export` ו-`named export` יחד.

ייבא את שניהם.

---

### רמה קשה

---

### תרגיל 9

צור 3 קבצים:

- `math.js`
- `utils.js`
- `app.js`

`utils` משתמש ב-`math`, ו-`app` משתמש ב-`utils`.

</div>
